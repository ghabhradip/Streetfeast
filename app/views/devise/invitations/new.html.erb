<center>
<div class="card bg-light mb-3" style="max-width: 26rem;margin-top: 5%;">
  <div class="card-header"><h2><%= t "devise.invitations.new.header" %></h2></div>
  <div class="card-body">
    <%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post} do |f| %>
      	<% resource.class.invite_key_fields.each do |field| -%>
        <div class="form-group">
        <br>
          <%= f.text_field field, class: 'form-control', placeholder: "Email" %></p>
        </div>
        <% end -%>
        <br>
         <p><%= f.submit t("devise.invitations.new.submit_button") , :class => "btn btn-primary btn_large"%></p>
    <%end%>
  </div>
</div>
</center>