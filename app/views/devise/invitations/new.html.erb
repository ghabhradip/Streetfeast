
<center>
<div class="card bg-light mb-3 invitation_card">
  <div class="card-header"><h2>Send Invitation</h2></div>
  <div class="card-body">
    <%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post} do |f| %>
      	<% resource.class.invite_key_fields.each do |field| -%>
        <div class="form-group">
        <br>
          <%= f.text_field field, class: 'form-control', placeholder: "Email",:required=>true %></p>
          <small id="emailHelp" class="form-text text-muted">Enter the email to send invitation</small>
        </div>
        <% end -%>
        <%= f.submit t("devise.invitations.new.submit_button") , :class => "medium_button"%>
    <%end%>
  </div>
</div>
</center>